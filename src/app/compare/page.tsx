import { CompareClient } from "./CompareClient";

export const metadata = {
  title: "Compare",
  description:
    "Like, skip, and pairwise-compare visual aesthetics to build your top 5.",
};

export default function ComparePage() {
  return <CompareClient />;
}
